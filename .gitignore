# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — set these in Vercel dashboard, NOT in repo
# Lovable bot has a history of committing wrong values here (see .env.example
# for the correct shape and the dnnxmjazillhktwttkux project ID).
.env
.env.local
.env.*.local
.env.development
.env.production

# Playwright test secrets — service role key, test user email. NEVER COMMIT.
.env.test

# Playwright auth state — contains live session cookies. Per-machine, not committed.
tests/e2e/fixtures/auth-state.json

# Playwright test artifacts
test-results/
playwright-report/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# WP1 eval harness run outputs (ephemeral)
evals/runs/
